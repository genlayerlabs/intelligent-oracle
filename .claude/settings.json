{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path'); if [[ \"$FILE\" == */intelligent-contracts/*.py ]]; then genvm-lint check \"$FILE\" 2>/dev/null || true; fi",
            "timeout": 30,
            "statusMessage": "Linting GenLayer contract..."
          }
        ]
      }
    ]
  }
}
